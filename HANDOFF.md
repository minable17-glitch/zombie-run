# Claude 인계 — 좀비런

작성일: 2026-09-26. 이 문서는 코드로 알 수 없는 작업 이력과 검증 경계를 기록한다. 과거 대화에서 “완료”라고 표현한 내용도 아래 검증 범위를 넘어서 해석하지 말 것.

## 1. 마지막 작업과 다음 단계

- 마지막 기능은 러닝 중 자동 화면 꺼짐 방지와 러닝 현황 PIP였다. 웹 기능은 커밋 `6c32b91`로 main에 push했고 GitHub Pages Actions `36216380656`의 성공을 확인했다. 공개 사이트: https://minable17-glitch.github.io/zombie-run/
- 화면 유지 요청 성공, 390×844 화면에서 제어 패널 표시, PIP 영상 준비와 실패 안내를 브라우저에서 확인했다. **실제 PIP 창이 뜨는 데 성공한 것은 아니다.** Codex 내장 브라우저는 지원 API를 노출했으나 창 열기 요청을 거절했다.
- 사용자가 진짜 원하는 것은 “휴대폰 화면을 완전히 꺼도 GPS·좀비 추격이 계속되고, 다른 앱 사용 중 작은 창으로 현황을 보는 것”이다. Android와 iPhone 둘 다 원한다고 답했다. 지금 웹 배포는 이 요구 전체를 충족하지 않는다.
- **설치형 Android/iOS 앱은 아직 만들지 않았다.** 백그라운드 추적, 백그라운드 게임 진행, 네이티브 PIP, 앱 서명, 스토어 등록 모두 미완료다. 웹사이트를 단순히 앱으로 감싸는 것만으로 해결된다고 설명하지 말 것.
- 다음 기술 작업은 실제 Android Chrome/iPhone Safari에서 현 웹 기능의 지원 여부를 확인하고, 설치형 앱의 위치 수신·게임 시간·좀비 상태·저장을 화면 생명주기와 분리하는 설계를 하는 것이다. Android부터 테스트 앱을 만들고 iOS를 잇는 방향을 제안했지만, 아직 네이티브 구현을 시작하지 않았다.
- 사용자는 최근 Play 등록 비용과 테스트 요건을 문의했다. 개발자 등록 결제, 스토어 계정 생성, 12명 테스트 모집을 진행했다고 볼 근거는 없다. 결제나 가입을 대신 진행하지 않았다.

## 2. 알고 있는 미해결 문제 — 영향 순

### 높음: 화면 잠금·앱 전환 중 러닝이 계속되지 않음
- 재현: 러닝을 시작하고 화면을 잠그거나 다른 앱으로 이동한다. 숨겨진 웹페이지의 게임 진행과 GPS 반영은 정지하도록 되어 있다. 복귀 후 새 GPS가 와야 이어진다.
- 이는 알려진 웹 제약에 맞춘 의도적 일시정지이며 사용자의 최종 요구에는 미달한다. 자동 화면 꺼짐 방지는 전원 버튼 잠금이나 백그라운드 GPS를 해결하지 않는다. PIP 역시 이를 해결하지 않는다.
- 실제 폰에서의 중단·복귀 시간과 동작은 미검증이다. 보이지 않는 동안 생존 시간을 임의로 늘리거나, 복귀 좌표로 미측정 거리를 더해서 해결하지 말 것.

### 중간: 지원 API가 있어도 PIP 요청이 실패하는 환경
- 재현한 환경: Codex 내장 브라우저, 로컬 가상 위치 러닝 → `화면 · PIP` → `PIP 준비` → `PIP 열기`.
- 영상 준비는 완료되었으나 창 열기는 거절되어 오류 안내가 나왔다. 구체적인 거절 원인 및 일반 Chrome/Safari와의 차이는 확정하지 못했다. Android/iPhone에서 작동한다고 주장하지 말 것.
- 오류를 표시하는 대응은 구현했으나 실제 기기 PIP 성공은 아직 확보하지 못했다. 현재 PIP 내용은 지도 전체가 아니라 시간·거리·좀비 거리 요약이다.

### 중간: 저장 전에 결과 화면을 떠나면 최종 기록 누락 가능
- 재현 조건: 네트워크를 끊은 상태에서 러닝 종료 후 저장 성공 전에 페이지를 닫는다.
- 결과 화면에서 재시도하지만 영속적인 오프라인 업로드 큐는 없다. 이는 기존 배포 문서에도 명시된 제한이다. 이 인계 때 실제 폰으로 재현한 것은 아니다.

### 나머지 검증 부채 / 알려진 제한
- 경로가 매우 짧거나 사용자가 지정 경로에서 멀리 떨어지면 후방 좀비 보충을 건너뛸 수 있다. “반환 후 서 있어도 살아남는 문제”의 현장 재검증은 필요하다. 자동 테스트 통과를 야외 검증으로 오해하지 말 것.
- 도로 경로가 사용자에게 닿지 않으면 직선 추격으로 전환한다. 실제 아파트 출입 가능 여부나 벽을 판별하는 기능은 아니다.
- 점수는 클라이언트 게임 결과를 서버에 전달한다. 소유권·시간 상한 등의 검증이 있어도 완전한 서버 권위 기반 부정행위 방지는 아니다.
- 이전 사용자 제보: 종료 후 다시 방 생성 시 “열려 있는 방을 먼저 닫아주세요”, 자유 모드에 기존 맵 경로가 적용됨, 지도 편집 중 두 번째 깃발 클릭 시 검은 화면, 이메일 복구 경로 문제. 수정 이력은 있으나 수정 후 실제 휴대폰에서 모두 다시 재현·통과했다고 확인할 근거는 없다. 현재 미수정으로 단정하지 말고 회귀 테스트 대상으로 취급한다.

## 3. 실제 Supabase 반영 상태

대상은 기존 좀비런 운영 프로젝트이며 **다른 앱과 Auth를 공유**한다. 공유 Site URL, 다른 앱 사용자, 공통 Auth 설정을 좀비런만의 것으로 보고 덮어쓰지 말 것. 프로젝트 식별자는 기존 배포 설정에서 확인할 수 있다. 이 문서에는 키나 인증 값을 넣지 않는다.

아래 “반영 확인”은 해당 시점의 SQL Editor 성공 응답 또는 기록에 근거한다. 이번 인계에서는 운영 SQL을 다시 실행하지 않았고 전체 DB를 새로 검사하지 않았다.

| 마이그레이션 | 운영 반영 근거 / 인계 상태 |
| --- | --- |
| `20260909140000_zombie_run_security.sql` | 반영 확인. 9/11 운영 보안 SQL 적용 및 공개 접근 차단 확인 기록 있음. |
| `20260910010000_room_expiration.sql` | 반영 확인. 9/11 운영 적용 및 만료 작업 등록 기록 있음. |
| `20260911030000_legacy_profile_lookup.sql` | **적용 여부 재확인 필요.** 로컬 파일과 이후 복구 성공 기록은 있지만, 현재 인계 근거에서 이 파일의 별도 SQL 실행 성공을 특정할 수 없음. 미적용이라고 단정하지도 말 것. |
| `20260916010000_map_boundary.sql` | 반영 확인. 9/16 운영 자유 구역 필드·검증 트리거 추가 성공 기록 있음. |
| `20260921010000_room_survival_rank.sql` | 반영 확인. 프런트 배포 전 SQL 적용 성공 기록 있음. |
| `20260922010000_personal_survival.sql` | 반영 확인. 9/22 운영 SQL 적용 기록 있음. |
| `20260922020000_finished_rooms.sql` | **적용 여부 재확인 필요.** 관련 프런트 배포/로컬 테스트와 별개로 이 파일의 운영 SQL 실행 결과를 현재 인계 근거만으로 확정할 수 없음. |
| `20260923010000_solo_maps.sql` | **적용 여부 재확인 필요.** 혼자 저장 맵 선택 프런트는 배포됐지만, 이 파일의 운영 SQL 적용 성공 증거를 현재 인계 근거만으로 확정할 수 없음. |
| `20260923020000_six_digit_codes.sql` | 반영 확인. 실제 SQL Editor에서 성공. 다만 기존 함수의 공백/개행 차이 때문에 최초 적용 때 시도 제한 삽입이 누락되어 아래 보완을 추가함. |
| `20260923021000_code_attempt_guard.sql` | 반영 확인. 실제 SQL Editor 적용 후 운영 함수 정의를 조회하여 6자리 생성과 닉네임별 시도 제한이 모두 포함된 결과 `true / true`를 확인함. |

- **명확하게 “미실행”으로 확인된 마이그레이션은 없지만, 위 3개는 반영 확인을 완료했다고 말할 수 없다.** 다음 담당자는 운영 함수/제약 정의와 파일을 대조하고 필요한 것만 적용해야 한다. 단순히 파일이 있거나 프런트 배포가 성공했다고 DB 반영 완료로 간주하지 말 것.
- SQL Editor로 수동 적용한 이력이 많다. CLI 마이그레이션 장부와 실제 객체 상태가 일치한다고 가정하지 말고, 빈 장부만 보고 전부 재실행하지 말 것.
- 과거 운영 기록: 익명 로그인 활성화, `zombie-auth` Edge Function v1 ACTIVE, 잘못된 로그인 요청 401, 방 만료 Cron 15분 간격 등록을 확인했다. 이번 인계 시점의 재조회는 하지 않았다.
- 이메일 복구는 과거 실제 수신→아이디 표시까지 확인했고, 비밀번호 재설정은 새 비밀번호 입력 화면까지만 확인했다. 사용자 비밀번호의 최종 변경 제출은 대신 하지 않았다.
- 운영 검증용 익명 계정/방/기록은 일부 보존돼 있다. 실제 사용자의 데이터와 구분 없이 정리하거나 삭제하지 말 것.
- GitHub Pages 배포는 프런트만 배포하며 SQL, Edge Function, Auth 설정을 자동 반영하지 않는다.

## 4. 실제 검증 범위

### 한 것
- 최신 전체 자동 테스트 **97개 통과**, 프로덕션 빌드 성공. GitHub Actions에서도 테스트·빌드·배포 성공 확인.
- 로컬에서 전체 병렬 실행 시 3개 테스트가 5초 제한을 초과했다. 실패 내용은 타임아웃이었고, `npm test -- --maxWorkers=2`로 재실행하여 97개 전부 통과했다. 타임아웃을 늘려 숨기지 않았다.
- DB 자동 테스트는 로컬 PGlite에서 역할·SQL 동작을 검증한다. 운영 Supabase Auth·메일 전송 검증을 대신하지 않는다.
- 데스크톱 브라우저의 가상 GPS와 390×844 화면 크기 검증. 화면 크기를 모바일로 바꾼 것은 실기기 테스트가 아니다.
- 과거 운영 연결 브라우저/독립 익명 세션으로 방 생성·참가·랭킹·종료 기록 저장 등을 확인한 기록이 있다. 좌표는 가상 GPS였다.
- 개인 코드 6자리 발급/변경, 이전 긴 코드 접속, 변경 후 기록 유지와 다른 세션 해제는 자동 테스트로 확인했다. 실제 사용자 개인 코드를 임의로 바꾸지 않았다.

### 안 한 것
- 개발 담당자가 실제 Android 또는 iPhone을 들고 야외에서 뛰며 GPS 정확도·좀비 추격·장시간 안정성을 검증한 적은 확인되지 않는다.
- 실제 휴대폰 진동 모터, 거리별 진동 주기, 절전 모드, 권한 거절/복구, 잠금/홈 전환/복귀, 실제 PIP 창 표시, 두 휴대폰의 동시 그룹 러닝은 미검증이다.
- 사용자가 과거 휴대폰 스크린샷과 버그를 제보했다는 사실만으로 수정 후 실기기 검증을 통과한 것으로 기록하지 말 것.
- 설치형 앱 빌드·설치·서명·스토어 심사·테스터 배포는 하지 않았다.

## 5. 코드만으로는 알기 어려운 의도와 이유

- 사용자가 말한 “자유 모드 닉네임/코드”는 별도 방을 만들라는 의미가 아니었다. **방 없이 생존 러닝을 하고, 자신의 최고 기록을 갱신하고 다른 사람과 경쟁**하는 것이 목적이었다. 혼자 달리기를 다시 방 생성 흐름으로 바꾸지 말 것.
- 다른 사람이 만든 맵도 그룹과 혼자 달리기에 활용하기를 원했다. 제작자만 수정/삭제하되 선택·플레이는 공유하는 방향이다.
- 자유 모드는 같은 위치에 저장 맵이 있어도 사용자 직접 추격이어야 한다. 사용자가 맵을 명시적으로 선택한 경우만 지정 경로를 사용한다.
- 첫 좀비는 기본 60초 뒤 등장한다. 사용자의 정정 요청은 “항상 즉시 등장”이 아니라 “원하면 즉시 등장시키는 버튼”이었다.
- 지정 경로 좀비가 앞쪽으로 지나가면 서 있어도 되는 문제 때문에 후방 보충을 도입했다. 길찾기가 사용자에게 닿지 않아 추격하지 못하는 문제 때문에 무조건 도로 경로에 묶는 대신 직선 추격 대안을 넣었다.
- 반환점 기능은 같은 길을 되돌아 달릴 때 좀비를 새 진행 방향의 뒤로 옮겨 다시 추격하게 하려는 목적이다. 현장 밸런스 검증은 남았다.
- 개인 코드가 어렵다는 피드백으로 새 코드는 숫자 6자리, 본인 변경 가능으로 바꿨다. 기존 긴 코드를 즉시 무효화하면 기존 기록 접속을 잃으므로 본인이 바꾸기 전까지 유지했다. 짧은 코드의 추측 위험 때문에 닉네임별 시도 제한도 보강했다.
- 시도 제한의 첫 SQL 패치가 운영 함수의 공백 차이에 실패했다. 이후 정규식과 설치 여부 검증을 추가한 후 실제 운영 정의를 확인했다. 후속 보완 파일을 생략하면 로컬과 운영 동작이 달라질 수 있다.
- 가상 GPS는 사용자가 컴퓨터에서 위치가 안 잡힌다고 해서 추가했으며 “실제에는 반영하지 말라”고 했다. 로컬 호스트에서만 활성화하고 개인 랭킹에 넣지 않는다. 공개 URL의 쿼리만으로 가상 점수를 만들 수 있게 확장하지 말 것.
- UI 참고는 사용자가 제공한 Break A Leg 이미지였다. 어두운 지도 중심, 간결한 게임 현황을 선호했고 이전 과도한 장식/캐릭터 시안을 마음에 들어 하지 않았다.
- 화면 유지와 PIP는 웹에서 가능한 보조 기능이다. PIP로 브라우저 백그라운드 제한을 우회했다고 홍보하면 안 된다. PIP 열기는 사용자 동작을 요구하므로 영상 준비 후 별도 열기 버튼을 뒀다.
- Supabase MCP 인증이 과거 실패하여 SQL Editor로 작업했다. Claude Code와 동시에 연결되어서 실패했다는 원인은 확인되지 않았다.

## 6. 환경변수 설정 여부 — 값 없음

2026-09-26 로컬 설정의 키 존재/비어 있지 않음과 GitHub secret 이름 목록만 확인했다. 값의 유효성까지 새로 검증한 것은 아니다.

| 항목 | 로컬 | GitHub Pages 빌드 |
| --- | --- | --- |
| `VITE_SUPABASE_URL` | `.env.local`에 설정됨 | 워크플로에 설정됨 |
| `VITE_SUPABASE_ANON_KEY` | `.env.local`에 설정됨 | 워크플로에 공개 클라이언트 키 설정됨 |
| `VITE_ORS_API_KEY` | `.env.local`에 없음 | `ORS_API_KEY` 저장소 secret 존재 확인. 빌드 변수로 연결돼 있음. 키 유효성/잔여 할당량 미확인 |
| `GH_PAGES` | 별도 로컬 설정 확인 안 됨 | 배포 빌드에 설정됨 |

- Edge Function의 `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`: 과거 운영 함수 작동 기록은 있으나 이번 인계에서 서버 환경변수 설정 목록을 직접 재확인하지 않았다. **현재 개별 설정 여부는 미확인**으로 인계한다. 클라이언트 변수로 옮기지 말 것.
- `ZR_ALLOWED_REDIRECTS`: 운영에서 명시적으로 설정했는지 미확인. 코드 기본값이 있으므로 미설정이어도 기본 동작이 가능하다. 공유 Auth의 전역 Site URL 변경으로 대체하지 말 것.
- `.env.local`은 git에서 제외되는 로컬 환경 설정이며 이번에도 push하지 않는다. 인계 요구에 따라 값은 문서에 쓰지 않았다. 다른 컴퓨터로 넘길 때는 해당 환경을 별도로 설정해야 한다.
- GitHub/Supabase 로그인 세션·CLI 인증은 다른 사람이나 새 환경으로 자동 이전되지 않는다. 만료되면 소유자가 다시 인증해야 한다.

## 7. 인계 직전 저장 상태

- 기능 코드의 마지막 커밋은 `6c32b91`이며 배포 성공을 확인했다.
- 인계 시작 시 tracked 변경과 untracked 소스 파일은 없었다. 이번 인계 문서도 별도 커밋 후 main에 push한다.
- 과거 `DEPLOYMENT.md`에는 당시 시점의 안내가 남아 있다. 예를 들어 초기 진동 간격이나 schema가 두 파일을 합쳤다는 설명은 이후 변경 전 기록이다. 현재 동작 및 최신 검증 경계는 이 문서와 최신 코드를 함께 확인할 것.
