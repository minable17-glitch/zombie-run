# 좀비 런 (GPS 좀비 추격 게임)

실제 GPS 위치를 이용해서, 실제로 뛰어야 좀비 무리를 따돌릴 수 있는 러닝 게임 프로토타입입니다.

배포 주소: https://minable17-glitch.github.io/zombie-run/

## 실행 방법

```bash
npm install
npm run dev
```

좀비가 도로를 따라오게 하려면 [OpenRouteService](https://openrouteservice.org/dev/#/signup)에서 무료 API 키를
발급받아 `zombie-run/.env.local`에 `VITE_ORS_API_KEY=발급받은키`로 넣어주세요 (git에는 안 올라감).
GitHub Pages 배포용으로는 저장소 Settings → Secrets and variables → Actions에 `ORS_API_KEY`라는 이름으로
등록해두면 배포 워크플로우가 자동으로 가져다 씁니다. 키가 없어도 게임은 정상 동작하고, 좀비는 직선으로만
쫓아옵니다.

관리자가 좀비 순찰 지도를 저장하려면 [Supabase](https://supabase.com) 무료 프로젝트가 하나 필요합니다.
자세한 건 아래 "관리자용 좀비 경로 만들기" 항목을 참고하세요. `.env.local`에 `VITE_SUPABASE_URL`,
`VITE_SUPABASE_ANON_KEY`를 넣고, 배포용으로는 같은 이름의 GitHub 저장소 시크릿을 등록하면 됩니다. 이것도
없으면 관리자 화면에서 저장만 안 될 뿐, 게임 자체(자유/제한구역 모드)는 그대로 잘 동작합니다.

Geolocation API는 보안 컨텍스트(HTTPS 또는 localhost)에서만 동작합니다.
`npm run dev`는 `@vitejs/plugin-basic-ssl`로 자체 서명 인증서를 사용해 https로 켜지므로,
같은 와이파이의 휴대폰에서 `https://<PC의 로컬 IP>:5173` 으로 접속하면 인증서 경고를 무시하고
GPS 테스트를 해볼 수 있습니다. 실제 배포 시에는 Vercel/Netlify 등 HTTPS를 지원하는 곳에 올리세요.

## 게임 규칙 (MVP)

- 시작 화면에서 목표 페이스(5'30"/6'00"/6'30"/7'00" per km)를 고릅니다. 좀비는 그 페이스 속도로 쫓아오고,
  화면 상단에는 내 실시간 페이스(최근 30초 기준)와 목표 페이스가 함께 표시됩니다 — 목표보다 느려지면
  좀비가 따라잡히기 시작해요.
- 시작 버튼을 누르면 위치 권한을 요청하고 바로 러닝이 시작됩니다.
- 시작 60초 뒤 좀비 1~2마리가 근처에 등장하고, 이후 90초마다 추가로 등장합니다 (동시에 최대 4마리까지만
  존재 — 러닝을 재밌게 만드는 정도의 긴장감이 목표라 무리를 크게 늘리지 않습니다).
- 좀비는 항상 **지금 달리고 있는 방향의 뒤쪽**에서만 등장하고, 아이템(탄약상자/모래시계)은 **앞쪽**에
  놓입니다. 그래서 방향을 이리저리 판단할 필요 없이 "그냥 계속 앞으로 달리기"가 곧 도망치는 방법이
  됩니다 (15m 이상 움직이기 전까지는 아직 달리는 방향을 몰라서 무작위 방향에서 등장해요).
- **관리자가 미리 좀비 경로를 만들어둔 장소**에서 시작하면(시작 화면 하단의 "🛠️ 관리자: 좀비 경로
  만들기"로 직접 만들 수 있어요) 동적 스폰 대신 그 경로를 그대로 씁니다. 좀비는 경로를 왕복 순찰하다가
  플레이어가 40m 안으로 들어오면 추격을 시작하고, 100m 이상 벌어지면 다시 순찰로 돌아갑니다. 자세한
  건 아래 "관리자용 좀비 경로 만들기" 항목을 참고하세요.
- 시작 화면에서 **자유 모드**(제한 없음) 또는 **제한구역 모드**(시작 위치 기준 300m~2km 반경)를 고를 수
  있습니다. 제한구역 모드에서는 좀비/아이템이 그 반경 안에서만 등장하고, 반경 밖에 누적 1시간 넘게
  있으면 생명이 1개씩 줄어듭니다.
- 좀비는 실제 플레이어 위치를 향해 다가옵니다. OpenRouteService API 키가 설정돼 있으면 실제 도로/인도
  경로를 따라 걸어오고(15초 또는 내가 60m 이상 움직일 때마다 경로 재계산), 키가 없거나 요청이 실패하면
  자동으로 직선 이동으로 대체됩니다. 12m 이내로 붙잡히면 체력(하트)이 1 깎입니다.
- 지도 위 좀비(🧟)를 탭하면 총알 1발을 소모해 처치합니다 (35m 이내에 있어야 함).
- 탄약 상자(📦)를 향해 걸어가면(15m 이내) 탄약 +4를 자동으로 얻습니다.
- 모래시계(⏳)를 얻으면 10초간 모든 좀비의 이동이 멈춥니다.
- 달린 거리에 비례해 오른쪽의 궁극기 게이지가 차오르고, 100%가 되면 탭해서 주변 200m 좀비를 한 번에 제거할 수 있습니다.
- 체력이 0이 되면 게임 오버, 언제든 "종료" 버튼으로 직접 러닝을 끝내고 결과(생존 시간·달린 거리·처치 수)를 볼 수 있습니다.

## 관리자용 좀비 경로 만들기

관리자가 앱 안에서 바로 지도를 그리고 저장하면, 배포/커밋 없이 다른 사람들이 바로 그 경로로
플레이할 수 있습니다. Supabase(무료) 프로젝트가 하나 필요합니다.

**최초 1회 설정**
1. https://supabase.com 에서 무료 회원가입 후 새 프로젝트를 만듭니다 (이름은 자유롭게, 예: zombie-run).
2. 왼쪽 메뉴 **SQL Editor**에서 이 저장소의 `supabase/schema.sql` 내용을 그대로 붙여넣고 실행합니다
   (좀비 지도를 저장할 테이블을 한 번만 만드는 작업).
3. **Settings → API**에서 **Project URL**과 **anon public** 키를 복사합니다.
4. 로컬 개발용: `zombie-run/.env.local`에 아래 두 줄을 추가합니다 (git에는 안 올라감).
   ```
   VITE_SUPABASE_URL=복사한 Project URL
   VITE_SUPABASE_ANON_KEY=복사한 anon 키
   ```
5. 배포용: GitHub 저장소 Settings → Secrets and variables → Actions에 같은 이름
   (`SUPABASE_URL`, `SUPABASE_ANON_KEY`)으로 시크릿을 등록합니다.

**사용법**
1. 시작 화면 맨 아래 "🛠️ 관리자: 좀비 경로 만들기"를 누릅니다 (위치 권한 필요).
2. 지도를 탭해서 점을 찍어 경로를 그리고, "이 경로 완료"로 저장합니다. 경로 하나당 좀비 1마리가
   그 위를 왕복 순찰합니다. 여러 경로(좀비)를 계속 추가할 수 있습니다.
3. 지도 이름과 플레이 반경을 정한 뒤 "이 지도 저장하기"를 누르면 바로 데이터베이스에 저장됩니다.

이후 그 반경 안에서 게임을 시작하면 자동으로 이 순찰 경로가 적용되고, 자유/제한구역 모드 선택은
무시됩니다(그 지도의 반경으로 제한구역 모드가 강제 적용됨). Supabase 설정이 없으면 관리자 화면에서
저장만 비활성화될 뿐, 게임 자체는 평소대로(자유/제한구역 모드) 동작합니다.

## 알려진 한계 (다음 단계 후보)

- 실제 GPS 오차(도심/실내에서 5~20m 수준) 때문에 붙잡힘 판정이 예민하게 느껴질 수 있어요. 필드 테스트 후
  `CATCH_RADIUS_M`/`SHOOT_RADIUS_M` 값을 조정하는 걸 권장합니다 (`src/App.jsx` 상단 상수).
- 화면이 꺼지거나 백그라운드로 가면(특히 iOS Safari) GPS 추적이 끊길 수 있습니다. 항상 화면을 켜둔 채
  플레이하는 걸 전제로 합니다.
- 백그라운드 추적, 설치형 PWA, 리더보드(Supabase 연동) 등은 아직 없습니다.
